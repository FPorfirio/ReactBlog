/* eslint-disable no-undef */
module.exports = {
  style: {
    postcss: {
      plugins: [
        // eslint-disable-next-line no-undef
        require('tailwindcss'),
        require('autoprefixer'),
      ],
    },
  },
}
